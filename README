FLUXION

A headless text engine forged in Rust.

---

BUILD & RUN
Ensure you have the latest Rust toolchain installed.

Clone the repository:
git clone git@github.com:0x-kys/fluxion

Run the TUI:
cargo run --release

The binary will be located at:
target/release/fluxion
